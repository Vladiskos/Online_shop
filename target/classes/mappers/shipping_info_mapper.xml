<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="billing_info_mapper">
    <select id="getById" resultMap="billing_info_map">
        SELECT
        *
        FROM billing_info b
        LEFT JOIN
        addresses a
        ON
        b.bi_id = a.addr_billing_info_id
        WHERE
        a.addr_id = #{id}
    </select>
    <select id="get" resultMap="billing_info_map">
        SELECT
        *
        FROM billing_info b
        LEFT JOIN
        addresses a
        ON
        b.bi_id = a.addr_billing_info_id
    </select>
    <delete id="deleteById">
        DELETE FROM billing_info WHERE bi_id=#{id}
    </delete>
    <insert id="create">
        INSERT INTO billing_info(bi_totalPrice, bi_payment_type, bi_information, bi_date_created, bi_date_modified, bi_addresses_id)
        VALUES(#{totalPrice},#{paymentType}, #{information}, #{created}, #{modified}, ${address.getId()});
    </insert>
    <update id="update">
        UPDATE billing_info SET
        bi_totalPrice = #{totalPrice},
        bi_information = #{information},
        bi_date_created = #{created},
        bi_date_modified = #{modified}
        WHERE bi_id=#{id}
    </update>

    <resultMap id="billing_info_map" type="com.java.automation.lab.fall.cehanovich.core22.domain.classes.info.BillingInfo" autoMapping="false">
        <id property="id" column="bi_id"/>
        <result property="totalPrice" column="bi_totalPrice"/>
        <result property="information" column="bi_information"/>
        <result property="created" column="bi_date_created"/>
        <result property="modified" column="bi_date_modified"/>
        <association property="address" resultMap="address_mapper.address_map" />

    </resultMap>

</mapper>